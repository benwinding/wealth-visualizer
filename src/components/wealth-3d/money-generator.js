const _100  = 100;
const _1k   = 1000;
const _10k  = 10000;
const _100k = 100000;
const _1M   = 1000000;
const _10M  = 10000000;
const _100M = 100000000;
const _1B   = 1000000000;
const _10B  = 10000000000;
const _100B = 100000000000;
const _1T   = 1000000000000;

export async function getItemsToDelete(currentItems, currentVal, newVal) {
  const itemsToRemove = this.addedCones.slice(countNew);
  return itemsToRemove;
}

export async function getItemsToAdd(addItems, newCount) {
  const currentCount = addItems.length;
  const shouldAdd = currentCount < newCount;
  if (!shouldAdd) {
    return [];    
  }
  if (newCount > _100) {
    _100
  }
}